export default function Home() {
  return (
    <main>
      <h1>AI-Powered Search</h1>
      <p>Run <code>npm run ingest</code> then <code>npm run search</code> to use the CLI.</p>
    </main>
  );
}
